{% extends 'base.html.twig' %}

{% block title %}Edit Chantier{% endblock %}

{% block body %}
<div class="page-content" style="margin-top: 35px">
        <div class="container-fluid">
            <h4 class="page-title" >Modifier le chantier # {{ chantier.id }}</h4>


            {{ include('chantier/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            
            {{ include('chantier/_delete_form.html.twig') }}
            <a class="btn btn-info btn-sm" style="float:right; margin-right:10px" href="{{ path('liste_chantiers') }}">Retour à la liste des chantiers</a>
        </div>
</div>

{% endblock %}
