{% extends 'base.html.twig' %}

{% block title %}Modifier employé {% endblock %}

{% block body %}
    <div class="page-content" style="margin-top: 35px">
        <div class="container-fluid">
            <h4 class="page-title" >Employé # {{ user.id }} : </h4>

            {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour' , 'class_name' : 'btn-warning'}) }}

            {{ include('user/_delete_form.html.twig') }}
            <a class="btn btn-info btn-sm" style="float:right; margin-right:10px" href="{{ path('liste_employes') }}">Retour à la liste des employés</a>

        </div>
    </div>
{% endblock %}
