{% extends 'base.html.twig' %}

{% block title %}Pointage{% endblock %}

{% block body %}
    <h1>Pointage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pointage.id }}</td>
            </tr>
            <tr>
                <th>Date_pointage</th>
                <td>{{ pointage.datePointage ? pointage.datePointage|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ pointage.duree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pointage_index') }}">back to list</a>

    <a href="{{ path('pointage_edit', {'id': pointage.id}) }}">edit</a>

    {{ include('pointage/_delete_form.html.twig') }}
{% endblock %}
